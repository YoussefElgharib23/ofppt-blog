{% extends 'base.html.twig' %}

{% block title parent() ~ ' | Create New Category' %}

{% block body %}
    {% set in_admin = true %}
    <div class="nk-content">
        <div class="container wide-xl">
            <div class="nk-content-inner">
                {{ include('partials/_sidebar.html.twig') }}
                <div class="nk-content-body">
                    <div class="nk-content-wrap">
                        <div class="nk-block-head nk-block-head-sm">
                            <div class="nk-block-head-content">
                                <h3 class="nk-block-title page-title">Create category</h3>
                                <div class="text-soft">
                                    <p>Create your new category.</p>
                                </div>
                            </div>
                        </div>
                        <div class="nk-block">
                            <div class="row g-gs">
                                <div class="col-xl-12 mx-auto">
                                    <div class="card card-bordered h-100">
                                        <div class="col-md-8 mx-auto card-inner">
                                            <div class="card-title">
                                                <div class="card-title text-center">
                                                    <h3>Create new category</h3>
                                                </div>
                                            </div>
                                            {{ form_start(form, {
                                                attr: {
                                                    'class': 'form-validate'
                                                }
                                            }) }}
                                            <div class="form-group">
                                                {{ form_row(form.name, {
                                                    label_attr: {
                                                        'class': 'form-label'
                                                    }
                                                }) }}
                                            </div>
                                            <div class="form-group">
                                                {{ form_row(form.status, {
                                                    label_attr: {
                                                        'class': 'form-label'
                                                    }
                                                }) }}
                                            </div>
                                            <button type="submit" class="actions-submit btn btn-dim btn-block btn-outline-danger">
                                                Add Category
                                                <em class="icon ni ni-plus-sm"></em>
                                            </button>
                                            {{ form_end(form) }}
                                        </div>
                                    </div>
                                </div><!-- .col -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    
    <script>
        $(document).ready(() => {
            $('select').select2();
        })
    </script>
    
{% endblock %}